Django Integration
==================

To use Sore Thumb in Django, add "sorethumb" to your list of INSTALLED_APPS. This is required to expose the `sorethumb` filter to template -- no models or views are included.